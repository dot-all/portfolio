---
interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<article
    class:list={[
        "bg-white p-6 rounded-xl shadow-sm border border-gray-100",
        className,
    ]}
>
    <slot />
</article>
